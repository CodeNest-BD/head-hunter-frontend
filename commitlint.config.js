/* eslint-disable @typescript-eslint/no-require-imports */ // Disable the rule for this file
module.exports = {
  rules: {
    "subject-case": [0], // Disable subject case rule
    "type-case": [0], // Disable type case rule
    "type-enum": [0], // Disable type enum rule
  },
  extends: ["@commitlint/config-conventional"],
};
